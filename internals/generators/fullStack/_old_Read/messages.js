/*
 * Read Messages
 *
 * This contains all the text for the Read container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.Read";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the Read container!"
  }
});
