/*
 * Update Messages
 *
 * This contains all the text for the Delete container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.Delete";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the Delete container!"
  }
});
