/* eslint-disable no-return-assign */
/* eslint-disable no-case-declarations */
/* eslint-disable comma-dangle */
/* eslint-disable no-console */
/*
 *
 * {{ upperCase name }} reducer
 *
 */

import { fromJS } from "immutable";
import {
  DEFAULT_ACTION,
  {{ upperCase name }}_CONST,
  {{ upperCase name }}_CONST_GET,
  {{ upperCase name }}_CONST_GET_ERROR,
  {{ upperCase name }}_CONST_GET_SUCCESS,
  {{ upperCase name }}_CONST_POST,
  {{ upperCase name }}_CONST_POST_ERROR,
  {{ upperCase name }}_CONST_POST_SUCCESS,
  {{ upperCase name }}_CONST_FORM_INPUT,
  {{ upperCase name }}_CONST_FORM_PLACE_HOLDER,
  {{ upperCase name }}_CONST_FORM_RESET
} from "./constants";

export const initialState = fromJS({
  {{ upperCase name }}_STATE_ID: "",
  {{ upperCase name }}_STATE_PAYLOAD: {},
  {{ upperCase name }}_STATE_SUCCESS: false,
  {{ upperCase name }}_STATE_ERROR: false,
  {{ upperCase name }}_STATE_GET_LOADING: false,
  {{ upperCase name }}_STATE_MODEL: "N0_MODEL",
  {{ upperCase name }}_STATE_INPUT: {},
  {{ upperCase name }}_STATE_PLACE_HOLDER: {},
  {{ upperCase name }}_STATE_FORM_RESET: false,
  {{ upperCase name }}_STATE_FORM_STRUCTURE: [],
  {{ upperCase name }}_STATE_POST_LOADING: false
});

function {{ name }}Reducer(state = initialState, action) {
  console.log("Global reducer file being called");
  switch (action.type) {
    case DEFAULT_ACTION:
      return state;
    case {{ upperCase name }}_CONST:
      console.log("in {{ upperCase name }}_CONST in REDUCER :: action::: ", action);
      console.log(
        "in {{ upperCase name }}_CONST in REDUCER :: action.struct ::: ",
        action.struct
      );
      console.log(
        "in {{ upperCase name }}_CONST in REDUCER :: action.model ::: ",
        action.model
      );
      const userInput = {};
      action.struct.map(each => (userInput[each.name] = ""));
      return state
        .set("{{ upperCase name }}_STATE_ID", action.id)
        .set("{{ upperCase name }}_STATE_MODEL", action.model)
        .set("{{ upperCase name }}_STATE_FORM_STRUCTURE", action.struct)
        .set("{{ upperCase name }}_STATE_INPUT", userInput);
    case {{ upperCase name }}_CONST_GET:
      console.log("in {{ upperCase name }}_CONST_GET in REDUCER :: action::: ", action);
      console.log(
        "in {{ upperCase name }}_CONST_GET in REDUCER :: action.id ::: ",
        action.id
      );
      console.log(
        "in {{ upperCase name }}_CONST_GET in REDUCER :: action.model ::: ",
        action.model
      );
      return state
        .set("{{ upperCase name }}_STATE_GET_LOADING", true)
        .set("{{ upperCase name }}_STATE_GET_ERROR", false);
    case {{ upperCase name }}_CONST_GET_ERROR:
      console.log(
        "in {{ upperCase name }}_CONST_GET_ERROR in REDUCER,:: error ::: ",
        action.error
      );
      return state.set("{{ upperCase name }}_STATE_GET_ERROR", action.error);
    case {{ upperCase name }}_CONST_GET_SUCCESS:
      console.log(
        "In {{ upperCase name }}_CONST_GET_SUCCESS in REDUCER,:: payload :::",
        action.payload
      );
      return state
        .set("{{ upperCase name }}_STATE_GET_LOADING", false)
        .set("{{ upperCase name }}_STATE_GET_ERROR", false)
        .set("{{ upperCase name }}_STATE_GET_SUCCESS", true)
        .set("{{ upperCase name }}_STATE_PLACE_HOLDER", action.payload);
    case {{ upperCase name }}_CONST_POST:
      console.log("in {{ upperCase name }}_CONST_POST in REDUCER :: action::: ", action);
      console.log(
        "in {{ upperCase name }}_CONST in REDUCER :: action.input ::: ",
        action.input
      );
      console.log(
        "in {{ upperCase name }}_CONST in REDUCER :: action.model ::: ",
        action.model
      );
      return state
        .set("{{ upperCase name }}_STATE_INPUT", action.input)
        .set("{{ upperCase name }}_STATE_POST_LOADING", true)
        .set("{{ upperCase name }}_STATE_POST_ERROR", false);
    case {{ upperCase name }}_CONST_POST_ERROR:
      console.log(
        "in {{ upperCase name }}_CONST_ERROR in REDUCER,:: error ::: ",
        action.error
      );
      return state
        .set("{{ upperCase name }}_STATE_POST_LOADING", false)
        .set("{{ upperCase name }}_STATE_POST_ERROR", action.error);
    case {{ upperCase name }}_CONST_POST_SUCCESS:
      console.log(
        "In {{ upperCase name }}_CONST_SUCCESS in REDUCER,:: payload :::",
        action.payload
      );
      return state
        .set("{{ upperCase name }}_STATE_POST_LOADING", false)
        .set("{{ upperCase name }}_STATE_POST_ERROR", false)
        .set("{{ upperCase name }}_STATE_POST_SUCCESS", true)
        .set("{{ upperCase name }}_STATE_PAYLOAD", action.payload);
    case {{ upperCase name }}_CONST_FORM_INPUT:
      console.log(
        "in {{ upperCase name }}_CONST_FORM_INPUT in REDUCER :: action::: ",
        action
      );
      console.log(
        "in {{ upperCase name }}_CONST_FORM_INPUT in REDUCER :: action.input ::: ",
        action.input
      );
      return state
        .set("{{ upperCase name }}_STATE_INPUT", action.input)
        .set("{{ upperCase name }}_STATE_ERROR", false);
    case {{ upperCase name }}_CONST_FORM_PLACE_HOLDER:
      console.log(
        "in {{ upperCase name }}_CONST_FORM_PLACE_HOLDER in REDUCER :: action::: ",
        action
      );
      console.log(
        "in {{ upperCase name }}_CONST_FORM_PLACE_HOLDER in REDUCER :: action.placeholder ::: ",
        action.placeholder
      );
      return state
        .set("{{ upperCase name }}_STATE_PLACE_HOLDER", action.placeholder)
        .set("{{ upperCase name }}_STATE_LOAD_RETRIEVED", true)
        .set("{{ upperCase name }}_STATE_ERROR", false);
    case {{ upperCase name }}_CONST_FORM_RESET:
      console.log(
        "in {{ upperCase name }}_CONST_FORM_RESET in REDUCER :: action.event ::: ",
        action
      );
      return state
        .set("{{ upperCase name }}_STATE_INPUT", {})
        .set("{{ upperCase name }}_STATE_FORM_RESET", true);

    default:
      return state;
  }
}

export default {{ name }}Reducer;
