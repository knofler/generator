/*
 * Start Messages
 *
 * This contains all the text for the Start container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.Start";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the {{ properCase name }} container!"
  }
});
